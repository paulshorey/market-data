# Database connection string
# For local development, use the public proxy URL from Railway dashboard
# For Railway deployment, this should reference the Postgres service: ${{Postgres.DATABASE_URL}}
DATABASE_URL=postgresql://postgres:password@localhost:5432/railway

# Server port (Railway injects this automatically)
PORT=3000
